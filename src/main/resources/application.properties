# 数据库配置（对应 GitHub Actions 中启动的 MySQL 服务）
spring.datasource.url=jdbc:mysql://localhost:3306/store?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=@Passw0rd
spring.datasource.driver-class-name=com.mysql.driver.Driver

# JPA 配置（如果你不需要 JPA 可以忽略，但为了示例加上）
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
